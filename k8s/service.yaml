apiVersion: v1
kind: Service
metadata:
  name: ecommerce-backend
  namespace: default
spec:
  selector:
    app: ecommerce-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
